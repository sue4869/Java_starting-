1. list : 순서가 있는 목록
List 인터페이스에 상속받는 것 : ArrayList 클래스, LinkedList 클래스
사용법은 같다. List 인터페이스를 똑같이 상속받은 것이기에 똑같이 add,get 메서드를 사용한다. 

2. set : 집합은 원소의 중복을 허용하지 않는다. 
hashSet 은 해쉬테이블에 원소를 저장, 원소들의 순서가 일정하지 않다는 단점 + 속도가 빠르다는 장점
treeSet : 값에 따라서 순서가 결정된다. hashSet보다는 느리다. - 정렬이 필요한 상황이 아니라면 hash를 이용하길
linkedHashSet : 삽입되었던 순서와 같다. 

3. map : 사전과 같은 자료구조
키(key)에 값(value)이 매핑된다. 
맵은 인터페이스가 다르기에 list와 set과 다른 메서드를 사용한다. 